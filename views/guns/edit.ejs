<%- include('../partials/head', { title }) %>
<%- include('../partials/navbar') %>

<main class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-7">
      <section class="panel card p-4 p-md-5">
        <div class="mb-4">
          <p class="eyebrow mb-2">Edit entry</p>
          <h1 class="h3 mb-2">Edit Gun</h1>
          <p class="text-secondary mb-0">Only the price and automatic status can change.</p>
        </div>

        <% if (error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/guns/<%= gun._id %>" method="POST" class="needs-validation" novalidate>
          <div class="mb-3">
            <label class="form-label" for="gunName">Gun Name</label>
            <input class="form-control" type="text" id="gunName" value="<%= gun.gunName %>" readonly />
            <div class="form-text">Gun name is locked after creation.</div>
          </div>

          <div class="mb-3">
            <label class="form-label" for="price">Price</label>
            <input class="form-control" type="number" id="price" name="price" min="0" step="0.01" required value="<%= gun.price %>" />
            <div class="invalid-feedback">Price is required.</div>
          </div>

          <div class="mb-3 form-check">
            <input class="form-check-input" type="checkbox" id="automatic" name="automatic" <%= gun.automatic ? 'checked' : '' %> />
            <label class="form-check-label" for="automatic">Automatic</label>
          </div>

          <button type="submit" class="btn btn-primary">Save Changes</button>
          <a href="/guns/<%= gun._id %>" class="btn btn-outline-secondary ms-2">Back</a>
        </form>
      </section>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
