<nav class="navbar navbar-expand-lg navbar-dark app-navbar sticky-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/guns">Gun Inventory</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navContent" aria-controls="navContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/guns">All Guns</a></li>
          <li class="nav-item"><a class="nav-link" href="/guns/new">Add Gun</a></li>
        <% } %>
      </ul>
      <div class="d-flex align-items-center gap-3 ms-auto text-end">
        <% if (currentUser) { %>
          <span class="user-pill">Hello, <%= currentUser.name %></span>
          <form action="/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-light btn-sm">Logout</button>
          </form>
        <% } else { %>
          <a class="btn btn-outline-light btn-sm" href="/login">Login</a>
          <a class="btn btn-accent btn-sm" href="/register">Register</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
